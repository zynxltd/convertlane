# Hero videos

Homepage uses rotating timelapse clips (`hero-bg-1.mp4` … `hero-bg-4.mp4`) with crossfade, Ken Burns, and progress dots.

Inner pages use a single loop (`hero-bg-1.mp4` by default).

Override in `.env`:

```env
BRAND_HERO_ROTATE_INTERVAL=5000
```
